function Heading({ label }: { label: string }) {
  return (
    <div className="font-SS text-3xl font-extrabold text-gray-900">
      <h1>{label}</h1>
    </div>
  );
}

export default Heading;
